/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { SSR_ORACLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_ORACLE_DISCRIMINATOR = 3;

export function getUpdateOracleDiscriminatorBytes() {
  return getU8Encoder().encode(UPDATE_ORACLE_DISCRIMINATOR);
}

export type UpdateOracleInstruction<
  TProgram extends string = typeof SSR_ORACLE_PROGRAM_ADDRESS,
  TAccountDataProviderAuthority extends string | AccountMeta<string> = string,
  TAccountOracle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDataProviderAuthority extends string
        ? ReadonlySignerAccount<TAccountDataProviderAuthority> &
            AccountSignerMeta<TAccountDataProviderAuthority>
        : TAccountDataProviderAuthority,
      TAccountOracle extends string
        ? WritableAccount<TAccountOracle>
        : TAccountOracle,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateOracleInstructionData = {
  discriminator: number;
  rho: bigint;
  chi: bigint;
  ssr: bigint;
};

export type UpdateOracleInstructionDataArgs = {
  rho: number | bigint;
  chi: number | bigint;
  ssr: number | bigint;
};

export function getUpdateOracleInstructionDataEncoder(): FixedSizeEncoder<UpdateOracleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['rho', getU128Encoder()],
      ['chi', getU128Encoder()],
      ['ssr', getU128Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ORACLE_DISCRIMINATOR })
  );
}

export function getUpdateOracleInstructionDataDecoder(): FixedSizeDecoder<UpdateOracleInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['rho', getU128Decoder()],
    ['chi', getU128Decoder()],
    ['ssr', getU128Decoder()],
  ]);
}

export function getUpdateOracleInstructionDataCodec(): FixedSizeCodec<
  UpdateOracleInstructionDataArgs,
  UpdateOracleInstructionData
> {
  return combineCodec(
    getUpdateOracleInstructionDataEncoder(),
    getUpdateOracleInstructionDataDecoder()
  );
}

export type UpdateOracleInput<
  TAccountDataProviderAuthority extends string = string,
  TAccountOracle extends string = string,
> = {
  dataProviderAuthority: TransactionSigner<TAccountDataProviderAuthority>;
  oracle: Address<TAccountOracle>;
  rho: UpdateOracleInstructionDataArgs['rho'];
  chi: UpdateOracleInstructionDataArgs['chi'];
  ssr: UpdateOracleInstructionDataArgs['ssr'];
};

export function getUpdateOracleInstruction<
  TAccountDataProviderAuthority extends string,
  TAccountOracle extends string,
  TProgramAddress extends Address = typeof SSR_ORACLE_PROGRAM_ADDRESS,
>(
  input: UpdateOracleInput<TAccountDataProviderAuthority, TAccountOracle>,
  config?: { programAddress?: TProgramAddress }
): UpdateOracleInstruction<
  TProgramAddress,
  TAccountDataProviderAuthority,
  TAccountOracle
> {
  // Program address.
  const programAddress = config?.programAddress ?? SSR_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    dataProviderAuthority: {
      value: input.dataProviderAuthority ?? null,
      isWritable: false,
    },
    oracle: { value: input.oracle ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.dataProviderAuthority),
      getAccountMeta(accounts.oracle),
    ],
    data: getUpdateOracleInstructionDataEncoder().encode(
      args as UpdateOracleInstructionDataArgs
    ),
    programAddress,
  } as UpdateOracleInstruction<
    TProgramAddress,
    TAccountDataProviderAuthority,
    TAccountOracle
  >);
}

export type ParsedUpdateOracleInstruction<
  TProgram extends string = typeof SSR_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    dataProviderAuthority: TAccountMetas[0];
    oracle: TAccountMetas[1];
  };
  data: UpdateOracleInstructionData;
};

export function parseUpdateOracleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateOracleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      dataProviderAuthority: getNextAccount(),
      oracle: getNextAccount(),
    },
    data: getUpdateOracleInstructionDataDecoder().decode(instruction.data),
  };
}
