/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { SSR_ORACLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CACHE_ORACLE_DATA_DISCRIMINATOR = 6;

export function getCacheOracleDataDiscriminatorBytes() {
  return getU8Encoder().encode(CACHE_ORACLE_DATA_DISCRIMINATOR);
}

export type CacheOracleDataInstruction<
  TProgram extends string = typeof SSR_ORACLE_PROGRAM_ADDRESS,
  TAccountOracle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracle extends string
        ? WritableAccount<TAccountOracle>
        : TAccountOracle,
      ...TRemainingAccounts,
    ]
  >;

export type CacheOracleDataInstructionData = { discriminator: number };

export type CacheOracleDataInstructionDataArgs = {};

export function getCacheOracleDataInstructionDataEncoder(): FixedSizeEncoder<CacheOracleDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: CACHE_ORACLE_DATA_DISCRIMINATOR })
  );
}

export function getCacheOracleDataInstructionDataDecoder(): FixedSizeDecoder<CacheOracleDataInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getCacheOracleDataInstructionDataCodec(): FixedSizeCodec<
  CacheOracleDataInstructionDataArgs,
  CacheOracleDataInstructionData
> {
  return combineCodec(
    getCacheOracleDataInstructionDataEncoder(),
    getCacheOracleDataInstructionDataDecoder()
  );
}

export type CacheOracleDataInput<TAccountOracle extends string = string> = {
  oracle: Address<TAccountOracle>;
};

export function getCacheOracleDataInstruction<
  TAccountOracle extends string,
  TProgramAddress extends Address = typeof SSR_ORACLE_PROGRAM_ADDRESS,
>(
  input: CacheOracleDataInput<TAccountOracle>,
  config?: { programAddress?: TProgramAddress }
): CacheOracleDataInstruction<TProgramAddress, TAccountOracle> {
  // Program address.
  const programAddress = config?.programAddress ?? SSR_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracle: { value: input.oracle ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.oracle)],
    data: getCacheOracleDataInstructionDataEncoder().encode({}),
    programAddress,
  } as CacheOracleDataInstruction<TProgramAddress, TAccountOracle>);
}

export type ParsedCacheOracleDataInstruction<
  TProgram extends string = typeof SSR_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracle: TAccountMetas[0];
  };
  data: CacheOracleDataInstructionData;
};

export function parseCacheOracleDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCacheOracleDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { oracle: getNextAccount() },
    data: getCacheOracleDataInstructionDataDecoder().decode(instruction.data),
  };
}
